import { Navbar } from "@/components/(layout)";

export default function insightPage(){
    return(
        <div>
            <Navbar display='fixed' />
            <h1>Insights</h1>
        </div>
    );
}